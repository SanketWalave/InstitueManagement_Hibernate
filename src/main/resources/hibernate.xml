<hibernate-configuration xmlns="http://www.hibernate.org/xsd/orm/cfg">
    <session-factory>
        <!-- JDBC Connection Properties -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/hibernate?useSSL=false&amp;allowPublicKeyRetrieval=true</property>
        <property name="hibernate.connection.username">root</property>
        <property name="hibernate.connection.password">root</property>

        <!-- Hibernate Dialect (for Hibernate 6/7) -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQLDialect</property>

        <!-- Schema generation -->
        <property name="hibernate.hbm2ddl.auto">update</property>
        <!-- options: validate | update | create | create-drop -->

        <!-- Debugging -->
<!--        <property name="hibernate.show_sql">true</property>-->
<!--        <property name="hibernate.format_sql">true</property>-->
    <mapping class="org.springjdbc.model.Student"/>
        <mapping class="org.springjdbc.model.Marks"/>
    </session-factory>

</hibernate-configuration>
